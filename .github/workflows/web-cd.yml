name: Web CD

on:
  workflow_run:
    workflows: ["Web CI"]
    types: [completed]
    branches: [master]

permissions:
  contents: read

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set deploy tag
        run: echo "TAG=$(echo ${{ github.event.workflow_run.head_sha }} | cut -c1-8)" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: cred-web
          file: cred-web/docker/prod.Dockerfile
          push: true
          tags: ${{ vars.DOCKERHUB_USERNAME }}/credwave-web:${{ env.TAG }}
          cache-from: type=gha,scope=cred-web-prod
          cache-to: type=gha,mode=max,scope=cred-web-prod
          build-args: |
            VITE_API_URL=${{ vars.VITE_API_URL }}
            VITE_POSTHOG_KEY=${{ vars.VITE_POSTHOG_KEY }}
            VITE_POSTHOG_HOST=${{ vars.VITE_POSTHOG_HOST }}
            VITE_PADDLE_VENDOR_ID=${{ vars.VITE_PADDLE_VENDOR_ID }}
            VITE_PADDLE_ENV=${{ vars.VITE_PADDLE_ENV }}
            VITE_PADDLE_PRICE_STARTER_MONTHLY=${{ vars.VITE_PADDLE_PRICE_STARTER_MONTHLY }}
            VITE_PADDLE_PRICE_STARTER_ANNUAL=${{ vars.VITE_PADDLE_PRICE_STARTER_ANNUAL }}
            VITE_PADDLE_PRICE_GROWTH_MONTHLY=${{ vars.VITE_PADDLE_PRICE_GROWTH_MONTHLY }}
            VITE_PADDLE_PRICE_GROWTH_ANNUAL=${{ vars.VITE_PADDLE_PRICE_GROWTH_ANNUAL }}
            VITE_PADDLE_PRICE_SCALE_MONTHLY=${{ vars.VITE_PADDLE_PRICE_SCALE_MONTHLY }}
            VITE_PADDLE_PRICE_SCALE_ANNUAL=${{ vars.VITE_PADDLE_PRICE_SCALE_ANNUAL }}

      - name: Copy compose to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "cred-web/docker/docker-compose.yml"
          target: "~/cred-web/"
          strip_components: 2

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: TAG,DOCKERHUB_USERNAME,DOCKERHUB_TOKEN
          script: |
            set -euo pipefail

            echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

            cd ~/cred-web

            export TAG="$TAG"
            export DOCKER_REGISTRY="$DOCKERHUB_USERNAME"

            envsubst < docker-compose.yml > docker-compose.resolved.yml

            docker stack deploy \
              --with-registry-auth \
              --prune \
              -c docker-compose.resolved.yml \
              cred-web_stack

            echo "Waiting for web service to stabilize..."

            MAX_WAIT=20
            WAIT_COUNT=0

            until [ $WAIT_COUNT -ge $MAX_WAIT ]; do
              REPLICAS=$(docker service ls \
                --filter name=cred-web_stack_web \
                --format '{{.Replicas}}')

              DESIRED=$(echo $REPLICAS | cut -d'/' -f2)
              RUNNING=$(echo $REPLICAS | cut -d'/' -f1)

              FAILED=$(docker service ps cred-web_stack_web \
                --filter "desired-state=running" \
                --no-trunc \
                --format '{{.CurrentState}}' \
                | grep -c "Failed" || true)

              if [ -n "$REPLICAS" ] && [ "$RUNNING" = "$DESIRED" ] && [ "$FAILED" -eq 0 ]; then
                echo "Service healthy."
                break
              fi

              echo "[$WAIT_COUNT/$MAX_WAIT] cred-web_stack_web not ready: $REPLICAS failed=$FAILED"
              sleep 10
              WAIT_COUNT=$((WAIT_COUNT + 1))
            done

            if [ $WAIT_COUNT -ge $MAX_WAIT ]; then
              echo "ERROR: Deployment timed out"
              docker service ls
              exit 1
            fi

            echo "Cleaning up old images..."
            docker image prune -f
        env:
          TAG: ${{ env.TAG }}
          DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
